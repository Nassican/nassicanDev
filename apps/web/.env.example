# Absolute base for canonical URLs, hreflang and OpenGraph.
# Use the final host: the apex currently redirects to www.
NEXT_PUBLIC_SITE_URL="https://www.nassican.com"

# Required. Published articles are read from the database, including during
# `next build`, where generateStaticParams decides what to prerender.
DATABASE_URL=""
DIRECT_DATABASE_URL=""

# Must match apps/admin: the panel authenticates to /api/revalidate with it
# after publishing, which is how a new article reaches this cache.
REVALIDATE_SECRET=""

# GA4 measurement id of the web data stream. Public by design: it ships in the
# page source. Scope it to Vercel's Production environment only - a value in
# Preview or Development would mix those sessions into the same property the
# admin dashboard reads.
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
