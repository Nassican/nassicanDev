import type { ProjectTranslation } from "../types";

/**
 * Only the tagline for now: the project is marked `comingSoon`, so the detail
 * page says the case study is on its way instead of rendering filler.
 * Add `summary`, `role`, `highlights` and `body` when the real information is
 * available, and drop the flag in `index.ts`.
 */
export const es: ProjectTranslation = {
  tagline: "Aplicación web con enfoque en rendimiento y accesibilidad.",
};
