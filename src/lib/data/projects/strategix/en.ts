import type { ProjectTranslation } from "../types";

/** See the note in `es.ts`: tagline only while the case study is unwritten. */
export const en: ProjectTranslation = {
  tagline: "Web application focused on performance and accessibility.",
};
